# v0.14 バックログ

> v0.13(検証・テンプレ拡充)完了時に作成。次セッションの Claude が拾って v0.14 着手に使う。
> 該当 ChangeLog エントリ作成時に削除予定(または「## 完了済」セクションに移送)。

---

## 完了済(参考)

- **v0.13**: 検証・テンプレ拡充 — ① `check.py` 出力改善(`--json` / `--color` / 行番号 + severity 分類)/ ② Phase 3.5 ゲート2補強(テストマニフェスト突合 + `_test_manifest.md` 雛形)/ ③ ぶら下がり依存テンプレ5本同梱(R-4/R-5/R-6/R-12/R-15、DSL 18本)。fixture `sample_v13_manifest` 追加。詳細は ChangeLog `[0.13.0]`
  - 確定した設計判断: 残テンプレは「全21本」ではなくぶら下がり依存を閉じる5本に絞った / severity は表示・JSON 分類用で exit code は不変(warning でも 1 件で exit 1)
- **v0.12**: 設計 repo ↔ 実装 repo 同期支援 — `sync-to-impl.sh` / 実装 repo スケルトン `impl_repo/` / ADR `origin` / `_sync_log.md` 同期台帳 / WORKFLOW.md §8 拡充。詳細は ChangeLog `[0.12.0]`

---

## v0.14 スコープ候補(優先度順)

### 中優先度

| # | 項目 | 概要 | 規模 |
|---|---|---|---|
| 1 | 残り日本語テンプレ事前同梱(続き) | DSL 未登録の R-2 / R-3 / R-10 / R-11、B-3〜B-19 残、D-1〜D-14 残 を順次追加(18本済 + 残り 約16本)。v0.13 と同じく「依存・参照頻度が高い順」で段階的に | 中(全部やるなら大) |
| 2 | DM-1 ドメインモデル機械検証 | 集約 `AG-` / 値オブジェクト `VO-` ↔ B-12 / B-13 整合(DDD 案件のみ効く) | 中 |
| 3 | `_sync_log.md` の機械検証 | ADR `origin: implementation` が `_sync_log.md` の逆同期メモに載っているか check.py で確認 | 小〜中 |

### 低優先度(後送り候補)

| # | 項目 | 概要 |
|---|---|---|
| 4 | depends_on 循環の重大度区別 | 自己ループを「仕様矛盾」とは別カテゴリ(warning)に切り分け |
| 5 | 残 10 本の英語テンプレ展開 | ユーザーニーズが見えてからで OK(英語は当面低優先) |
| 6 | `CLAUDE_en.md` / `00_intake_en.md` 並列 | 同上 |
| 7 | sync-to-impl.sh の逆方向(impl → design) | 実装 repo で生まれた `origin: implementation` の ADR を設計 repo に取り込む支援 |
| 8 | `check.py --code-sync` | 実装コードと設計書の命名整合検証。コード解析が言語依存で重い・ROI 要検証 |

---

## 次セッション開始時の流れ

```
1. python harness/tools/check.py --session-start output   (案件作業時)/ 本ファイルを読む(ハーネス開発時)
2. このファイル(v0.14-backlog.md)を読む
3. 「中優先度」から v0.14 のテーマを提示し、ユーザー承認を取る
4. タスク分解 → 順次実装
5. 完了後 ChangeLog に v0.14 エントリ + 本ファイル更新(v0.15-backlog.md へ)
```
