# v0.13 バックログ

> v0.12(設計 ↔ 実装 同期支援)完了時に作成。次セッションの Claude が拾って v0.13 着手に使う。
> 該当 ChangeLog エントリ作成時に削除予定(または「## 完了済」セクションに移送)。

---

## 完了済(参考)

- **v0.12**: 設計 repo ↔ 実装 repo 同期支援 — `sync-to-impl.sh` / 実装 repo スケルトン `impl_repo/`(CLAUDE_impl.md / docs_design_README.md / 読取専用 pre-commit)/ ADR `origin` フィールド / `_sync_log.md` 同期台帳 / WORKFLOW.md §8 拡充(submodule 手順 + 影響レベル分類)。詳細は ChangeLog `[0.12.0]`
  - 確定した設計判断: `.gitmodules` はコマンド例のみ / 読取専用は CLAUDE_impl.md + 軽量ガード / new-project.sh は手順化のみ

---

## v0.13 スコープ候補(優先度順)

### 中優先度(v0.12 で見送ったもの)

| # | 項目 | 概要 | 規模 |
|---|---|---|---|
| 1 | `check.py` 出力改善 | 行番号付き / 重大度の色分け(`--color`)/ JSON 出力モード(`--json` で CI 連携しやすく) | 小〜中 |
| 2 | Phase 3.5 ゲート2 補強 | `_handoff_to_implementation/_test_manifest.md` と TS-1 / D-15 の `UT-` / `AT-` を突合(TDD 採用案件向け) | 中 |
| 3 | 残り日本語テンプレ事前同梱 | DSL(`harness/spec/templates.py`)未登録の R/B/D 系を順次追加(18 本済 + 残り 約21 本) | 中(全部やるなら大) |

### 低優先度(後送り候補 / v0.14+)

| # | 項目 | 概要 |
|---|---|---|
| 4 | DM-1 ドメインモデル機械検証 | 集約 `AG-` / 値オブジェクト `VO-` ↔ B-12 / B-13 整合(DDD 案件のみ効く) |
| 5 | depends_on 循環の重大度区別 | 自己ループを「仕様矛盾」とは別カテゴリに切り分け |
| 6 | 残り 10 本の英語テンプレ展開 | ユーザーニーズが見えてからで OK(英語は当面低優先) |
| 7 | `CLAUDE_en.md` / `00_intake_en.md` 並列 | 同上 |
| 8 | `check.py --code-sync` | 実装コードと設計書の命名整合検証。コード解析が言語依存で重い・ROI 要検証 |

### v0.12 同期支援の後続候補(運用して気づいたら)

| # | 項目 | 概要 |
|---|---|---|
| 9 | `_sync_log.md` の機械検証 | ADR `origin: implementation` が `_sync_log.md` の逆同期メモに載っているか check.py で確認 |
| 10 | sync-to-impl.sh の逆方向(impl → design) | 実装 repo で生まれた `origin: implementation` の ADR を設計 repo に取り込む支援 |

---

## 次セッション開始時の流れ

```
1. python harness/tools/check.py --session-start output
2. このファイル(v0.13-backlog.md)を読む
3. 「中優先度」から v0.13 のテーマを提示し、ユーザー承認を取る
4. タスク分解 → 順次実装
5. 完了後 ChangeLog に v0.13 エントリ + 本ファイル更新
```
