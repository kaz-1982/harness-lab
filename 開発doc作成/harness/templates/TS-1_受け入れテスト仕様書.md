---
doc_id: TS-1
doc_name: 受け入れテスト仕様書
phase: テスト
required_when: テスト戦略=TDD かつ AC定義あり
depends_on: [R-7 or R-8, B-8]
mode: 両対応
---

# {{案件名}} - 受け入れテスト仕様書(TS-1)

> **原典外・ハーネス拡張ドキュメント**(`input/システム開発設計ドキュメント一覧.md` に対応節は無い)。テスト戦略 = TDD の案件でのみ Phase 3.5 で作成。受け入れ基準(AC)を入力に、利用者視点の振る舞いを Given-When-Then で表現し、各ケースに `AT-*` を採番する。ATDD(受け入れテスト駆動開発)の起点となる。

## 1. 目的
ユーザーストーリー(R-7)/ 機能要件(R-8)の **受け入れ基準(Acceptance Criteria)** が満たされることを、利用者視点で検証する受け入れテストケースを設計する。

## 2. 適用範囲・スコープ
- 含むもの: {{対象ストーリー / 機能の AC、画面操作を伴う業務フロー}}
- 含まないもの: {{モジュール内部ロジック(D-15 単体テストで扱う)、性能・負荷(非機能テストで扱う)}}

## 3. 前提・依存
- 参照ドキュメント: R-7 ユーザーストーリー(の AC)/ R-8 機能要件, B-8 画面設計書, B-6 機能一覧
- 採番済ID: {{対象ストーリー、機能ID(F-*)、画面ID(S-*)}}
- テストフレームワーク: {{受け入れ/E2E 用。例 Cucumber / Playwright / RSpec feature}}

## 4. 本文

### 4.1 対象の受け入れ基準(AC)一覧
| AC元 | ストーリー/機能ID | 受け入れ基準(要約) |
|---|---|---|
| R-7 | {{US-xx / F-xxx}} | {{AC の要約}} |

### 4.2 受け入れテストケース(Given-When-Then)
1ケース1行。各ケースに `AT-*` を採番。
期待結果(Then)は検証可能に書く。

| テストID | 対応AC | Given(前提) | When(操作) | Then(期待結果) |
|---|---|---|---|---|
| AT-001 | {{F-xxx の AC}} | {{前提状態}} | {{利用者の操作}} | {{観測可能な結果}} |

### 4.3 テストデータ・環境前提
{{受け入れテストに必要なデータセット、ユーザーロール、初期状態}}

## 5. 関連トレーサビリティ
- 上流(本書の根拠): R-7/R-8 の AC, B-8 画面設計, B-6 機能一覧
- 下流(本書を参照): Phase 3.5 Red テストコード(`output/_handoff_to_implementation/tests/acceptance/`), R-14 RTM のテストID列(AT-*)

## 6. 改訂履歴
| 版 | 日付 | 変更者 | 変更内容 |
|---|---|---|---|
| 0.1 | YYYY-MM-DD | | 初版作成(Phase 3.5 ゲート1) |

## 7. レビュー状態
- 単体品質チェック: 未 / 実施済(YYYY-MM-DD)
- 整合性チェック: 未 / 実施済(YYYY-MM-DD)
- ゲート1(テスト仕様レビュー): 未 / 承認(承認者、日付)
- TBD / 残課題:
  - (なし、または箇条書き)
- 承認: 未 / 承認(承認者、日付)

---

### 補足: 記入例(参考)

> 元 AC(R-7): 「経理担当として、月次決算を早期化するために、売掛金データをボタン1つで CSV 出力したい。受け入れ基準: 出力時間3秒以内、文字コード UTF-8、ヘッダー行付き。」
>
> | テストID | 対応AC | Given | When | Then |
> |---|---|---|---|---|
> | AT-001 | F-010 CSV出力 | 売掛金データが100件存在し経理ロールでログイン済 | 「CSV出力」ボタンを押下 | 3秒以内に UTF-8・ヘッダー行付き CSV がダウンロードされる |
> | AT-002 | F-010 CSV出力 | 売掛金データが0件 | 「CSV出力」ボタンを押下 | ヘッダー行のみの CSV が出力される |
