---
doc_id: PR-1
doc_name: 事前検討サマリ
phase: 事前検討
required_when: input/事前検討資料/ に資料が投入されている
depends_on: []
mode: 両対応
---

# {{案件名}} - 事前検討サマリ(PR-1)

> **原典外・ハーネス拡張ドキュメント**(`input/システム開発設計ドキュメント一覧.md` に対応節は無い)。要件定義の前段(Phase 0)で、`input/事前検討資料/` に投入された「問題・原因・対応策」を検討した資料を読み込み、内容を正規化したもの。**ここに書く想定要件はあくまで要件候補のシード**であり、要件としての確定は Phase 1(要件定義)で行う。資料が無い案件ではこのドキュメントは作成しない。

## 1. 目的
要件定義に入る前に検討された「問題 → 原因 → 対応策」を整理し、以降のインテーク・要件定義(R-1 / R-7 / R-8)の入力シードとする。
検討の出発点(なぜこのシステムを作るのか)を1か所に固定し、要件への追跡性を確保する。

## 2. 適用範囲・スコープ
- 含むもの: `input/事前検討資料/` に投入された資料から読み取れる問題・原因・対応策・想定要件候補
- 含まないもの: 要件の確定(Phase 1 で実施)、設計・実装方針の詳細

## 3. 前提・依存
- 参照した事前検討資料(`input/事前検討資料/` 配下):
  | ファイル名 | 形式 | 概要 |
  |---|---|---|
  | {{ファイル名}} | {{md/docx/pdf/...}} | {{1行概要}} |
- 読み取れなかった / 口頭補足を受けた点: {{あれば記載。なければ「なし」}}

## 4. 本文

### 4.1 問題(課題)
現状で何が問題か。
事実ベースで記述する。
- {{問題1}}
- {{問題2}}

### 4.2 原因
4.1 の問題がなぜ起きているか(根本原因)。
問題と原因の対応が分かるように書く。
- {{問題1 の原因}}
- {{問題2 の原因}}

### 4.3 対応策
問題・原因に対して資料で検討された打ち手・方針。
- {{対応策1}}
- {{対応策2}}

### 4.4 想定要件・制約・前提(要件候補のシード)
4.3 の対応策から導かれる、システムへの要求・制約・前提条件の **候補**。
確定は Phase 1。
- 想定業務要件(→ R-1 候補): {{...}}
- 想定機能(→ R-7 / R-8 候補): {{...}}
- 想定非機能(→ R-9 候補): {{...}}
- 制約・前提: {{...}}

### 4.5 未確定事項 / インテークで確認すること
資料だけでは決められない点を TBD として列挙(推測で埋めない=ルール7)。
- **TBD**: {{確認事項}}

## 5. 関連トレーサビリティ
- 上流(本書の根拠): `input/事前検討資料/` の投入資料
- 下流(本書を参照する要件・設計): R-1 業務要件定義書 / R-7 ユーザーストーリー or R-8 機能要件定義書 / R-9 非機能要件定義書(各書の「5. 関連トレーサビリティ」上流に PR-1 を記載)
- 対応策 → 想定要件候補 の対応:
  | 対応策 | 想定要件候補 | 確定先(Phase 1 で更新) |
  |---|---|---|
  | {{対応策1}} | {{想定要件}} | {{R-x / 未確定}} |

## 6. 改訂履歴
| 版 | 日付 | 変更者 | 変更内容 |
|---|---|---|---|
| 0.1 | YYYY-MM-DD | | 初版作成(Phase 0 / 事前検討資料から正規化) |

## 7. レビュー状態
- 単体品質チェック: 未 / 実施済(YYYY-MM-DD)
- 整合性チェック: 未 / 実施済(YYYY-MM-DD)
- ユーザーとの認識合わせ: 未 / 実施済(YYYY-MM-DD)
- TBD / 残課題:
  - (なし、または箇条書き)
- 承認: 未 / 承認(承認者、日付)

---

### 補足: 記入例(参考)

> **4.1 問題**: 月次決算が締め日から完了まで10営業日かかり、経営判断が遅れている。
> **4.2 原因**: 売掛金の消し込みが手作業のExcel突合で、担当者1名に属人化している。
> **4.3 対応策**: 入金データと請求データを自動マッチングする消し込み機能を新規構築し、突合作業を自動化する。
> **4.4 想定要件候補**: 入金CSV取込(→R-8候補)/ 自動マッチングルール設定(→R-8候補)/ 決算リードタイム5営業日以内(→R-9候補)。
> **4.5 TBD**: マッチング不一致時の手動補正フローの要否(インテークで確認)。
