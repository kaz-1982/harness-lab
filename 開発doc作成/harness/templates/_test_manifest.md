# テストマニフェスト(Phase 3.5 ゲート2 / TDD採用時)

> このファイルは Phase 3.5 ゲート2(Red コード引き渡し)で、Claude が
> `output/_handoff_to_implementation/_test_manifest.md` として生成する雛形です。
> テストID(UT-* / AT-*)↔ テストファイル ↔ 対応する要件・設計 の対応を一覧化し、
> 実装(Phase 4)側がトレーサビリティを保ったまま Green 化できるようにします。

> **machine check(v0.13〜)**: `harness/tools/check.py` の「テストマニフェスト突合」が、
> 本表の `UT-*` / `AT-*` と D-15(単体テスト仕様)/ TS-1(受け入れテスト仕様)の定義を突合します。
> - 掲載漏れ: D-15 / TS-1 で定義した ID が本表に無い → 引き渡し漏れ
> - ゴースト: 本表に書いた ID が D-15 / TS-1 に無い → 定義のないテスト
> プレースホルダ行(`{{ }}` を含む行)はスキップされます。本番では全行を実IDに置き換えてください。

---

## 1. 技術スタック・テストフレームワーク

- 言語 / ランタイム: {{例: Python 3.12}}
- テストフレームワーク: {{例: pytest 8.x}}
- 実行コマンド: {{例: pytest tests/ -v}}

## 2. 単体テスト(UT-* / D-15 由来)

| テストID | テストファイル | 対象モジュール | 対応要件/設計 | 状態 |
|---|---|---|---|---|
| {{UT-001}} | {{tests/unit/test_xxx.py::test_yyy}} | {{UserService.register}} | {{R-F-001 / D-12}} | {{Red}} |

## 3. 受け入れテスト(AT-* / TS-1 由来)

| テストID | テストファイル | シナリオ | 対応要件/AC | 状態 |
|---|---|---|---|---|
| {{AT-001}} | {{tests/acceptance/test_zzz.py::test_www}} | {{注文を確定できる}} | {{R-F-001 のAC1}} | {{Red}} |

## 4. トレーサビリティ整合

- `output/横断/R-14_RTM.md` のテストID列(単体テスト / 受入テスト)と本表を一致させること。
- 本表の全 `UT-*` は D-15 に、全 `AT-*` は TS-1 に定義済みであること(`check.py` が突合)。

## 5. 引き渡し時の状態

- 全テストが **Red(失敗)** であることを確認した上で引き渡す(出発点)。
- 実装側は「状態」列を Green 化の進捗管理に流用してよい(引き渡し時点は全 Red)。
